function apartmentHunting(blocks, requireMent) {
  let hash = {};

  for (const block in blocks) {
    hash.block = {};
    for (const building in block) {
      if (hash[block]) {
      }
    }
    arr.sort(
      (function (index) {
        return function (a, b) {
          return a[index] === b[index] ? 0 : a[index] < b[index] ? -1 : 1;
        };
      })(2)
    );
  }
}
